{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core defaults and write-only clipboard. URLs and folders are opened only through validated app commands; the webview gets no opener or clipboard-read permission.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "clipboard-manager:allow-write-text"
  ]
}
